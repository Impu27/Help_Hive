// ===== src/environments/environment.ts =====
export const environment = {
  production: false,
  apiUrl: '/api',  // API endpoints
  serverUrl: 'http://localhost:3000'  // Full server URL for file serving (matches backend PORT in .env)
};